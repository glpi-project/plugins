'use strict';

angular.module('frontendApp')
   // API url
   .constant('API_URL', 'http://glpiplugindirectory/api')
   // Recaptcha Public Key
   .constant('RECAPTCHA_PUBLIC_KEY', '--the recaptcha key here--')
   // Client id of github app
   .constant('GITHUB_CLIENT_ID', '--the github client id here--')
;